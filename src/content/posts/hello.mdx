---
title: Hola, hidx
description: Un primer artículo que ejercita cada componente MDX para verlos renderizados.
publishedAt: 2026-04-28
tags: [meta, astro]
---

import { Callout, Steps, CodeDemo, CodeDiff, CodeTabs, Terminal, Mermaid, AffiliateLink, TipJar, Image, Video, YouTubeEmbed, Gallery, Figure, Quote, Aside, FullBleed, Spoiler, KeyboardKey, Highlight } from '../../components/mdx';

## Por qué existe este artículo

Este es un artículo de prueba. Existe para verificar que el pipeline Astro + MDX renderiza correctamente,
que los componentes están conectados y que el layout del artículo funciona de punta a punta.

## Un callout

<Callout type="info" title="Lo que aprenderás">
  Cómo funciona el resto de los componentes en esta página — y por qué cada uno existe.
</Callout>

## Un walkthrough de varios pasos

<Steps>
  <div class="steps-step">
    <h3>Instalar dependencias</h3>
    <p>Ejecuta <code>pnpm install</code>.</p>
  </div>
  <div class="steps-step">
    <h3>Iniciar el servidor de desarrollo</h3>
    <p>Ejecuta <code>pnpm dev</code> y abre <code>http://localhost:4321</code>.</p>
  </div>
  <div class="steps-step">
    <h3>Editar y recargar</h3>
    <p>Astro recarga en caliente al guardar.</p>
  </div>
</Steps>

## Una demo de código con resaltado de sintaxis

<CodeDemo
  lang="ts"
  title="src/lib/saludo.ts"
  code={`export function saludo(nombre: string): string {
  return \`hola, \${nombre}\`;
}`}
/>

## Tabs de código multi-archivo

<CodeTabs
  tabs={[
    {
      label: 'package.json',
      lang: 'json',
      code: `{
  "name": "hidx",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build"
  }
}`,
    },
    {
      label: 'astro.config.mjs',
      lang: 'js',
      code: `import { defineConfig } from 'astro/config';
import mdx from '@astrojs/mdx';

export default defineConfig({
  integrations: [mdx()],
});`,
    },
    {
      label: 'src/pages/index.astro',
      lang: 'astro',
      code: `---
const title = 'hidx';
---
<h1>{title}</h1>`,
    },
  ]}
/>

## Diff de código

<CodeDiff
  title="src/lib/theme.ts"
  code={`-export type Theme = 'dark';
+export type Theme = 'light' | 'dark';

 export function applyTheme(theme: Theme): void {
   document.documentElement.dataset.theme = theme;
+  localStorage.setItem('theme', theme);
 }`}
/>

## Diagrama Mermaid

<Mermaid
  caption="Pipeline de build hidx con cache."
  code={`flowchart LR
    A[MDX Source] --> B{Tiene Mermaid?}
    B -- Sí --> C[Render lazy on view]
    B -- No --> D[HTML estático]
    C --> E[SVG inline]
    D --> F[CF Pages edge]
    E --> F
    F --> G((Reader))`}
/>

## Sesión de terminal

<Terminal
  title="bootstrap hidx"
  lines={[
    { type: 'comment', text: 'Clonar e instalar' },
    { type: 'cmd', text: 'git clone git@github.com:felipet1987/hidx.git' },
    { type: 'cmd', text: 'cd hidx && pnpm install' },
    { type: 'out', text: 'Done in 22.4s using pnpm v10.33.2' },
    { type: 'cmd', text: 'pnpm dev' },
    { type: 'out', text: '┃ Local    http://localhost:4321/' },
    { type: 'comment', text: 'Listo. Editar src/ recarga en caliente.' },
  ]}
/>

## Una imagen optimizada

<Image
  src="https://images.unsplash.com/photo-1518770660439-4636190af475?w=1280&q=80"
  alt="Placa de circuito impreso vista de cerca"
  caption="Image — wrapper de Astro Image con AVIF/WebP, lazy y caption."
  width={1280}
  height={720}
/>

## Un YouTube embed con privacidad

<YouTubeEmbed
  id="aircAruvnKk"
  title="But what is a neural network? — 3Blue1Brown"
/>

## Una galería con lightbox

<Gallery
  cols={3}
  items={[
    { src: 'https://images.unsplash.com/photo-1518770660439-4636190af475?w=600&q=80', alt: 'Circuito 1', caption: 'Detalle macro' },
    { src: 'https://images.unsplash.com/photo-1526374965328-7f61d4dc18c5?w=600&q=80', alt: 'Pantalla con código', caption: 'Editor a oscuras' },
    { src: 'https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=600&q=80', alt: 'Más código', caption: 'Más código' },
  ]}
/>

## Una figure semántica

<Figure caption="Figure — wrapper semántico para cualquier media + caption + source." source="hidx" sourceUrl="https://hidx.dev">
  <pre style="padding:1rem;background:var(--color-bg-elev);border-radius:8px;">
    {`<Figure /> envuelve cualquier contenido y agrega <figcaption> + atribución opcional.`}
  </pre>
</Figure>

## Un enlace de afiliado

Si necesitas un proveedor de hosting, yo uso <AffiliateLink href="https://www.cloudflare.com/">Cloudflare</AffiliateLink> para todo en hidx.

## Una caja de propinas

<TipJar provider="ko-fi" handle="felipet1987" />

## Una pull-quote

<Quote cite="Edsger Dijkstra" source="The Humble Programmer" sourceUrl="https://www.cs.utexas.edu/users/EWD/transcriptions/EWD03xx/EWD340.html">
  Simplicity is prerequisite for reliability.
</Quote>

## Una nota al margen

<Aside title="Tip de craft">
  En desktop ≥1024px, este bloque flota a la derecha del párrafo. En mobile vuelve inline. Es una técnica clásica de revistas impresas.
</Aside>

Esto demuestra cómo la nota lateral convive con el párrafo principal sin romper el flow del lector.
La nota aparece a la derecha porque la viewport es ancha; en celular se acomoda inline.

## Atajos de teclado

Para abrir tabs nuevas, usá <KeyboardKey combo="Cmd + T" />.
Para mover entre archivos, <KeyboardKey combo="Ctrl + P" /> o <KeyboardKey combo="Cmd + Shift + O" />.
También funciona la tecla individual: <KeyboardKey>Enter</KeyboardKey>.

## Texto resaltado

Esta línea contiene <Highlight variant="yellow">una palabra crítica</Highlight> destacada,
y esta otra muestra <Highlight variant="cyan">un valor cyan</Highlight> distinto.

## Spoiler colapsable

<Spoiler summary="¿Por qué Astro 6 cambió el content layer?">
  Para soportar loaders custom (DB, API, file system), Astro 6 movió el config a `src/content.config.ts`
  y exige un `loader` por collection. La API anterior (`src/content/config.ts` con `glob`) sigue funcionando
  vía adapter pero está marcada como legacy.
</Spoiler>

## Una imagen full-bleed

<FullBleed>
  <Image
    src="https://images.unsplash.com/photo-1483728642387-6c3bdd6c93e5?w=1920&q=80"
    alt="Paisaje montañoso al amanecer"
    width={1920}
    height={720}
  />
</FullBleed>

## Cierre

Si puedes leer esto con los componentes renderizados, el pipeline funciona.
